
Проект реализует **многослойную архитектуру** для управления данными об офисных зданиях с поддержкой **CRUD-операций**, **Dependency Injection (Hilt)**, **ROOM-базы данных** и **тестирования**.  
Исходная логика взята из задачи по объектно-ориентированному программированию:  
> *«Офисные здания определяются адресом. При наличии стоянки — количеством машиномест и стоимостью аренды в месяц. Выведите здания, в адресе которых есть слово “Тверская”».*

---

## 📁 Структура проекта

<img width="476" height="840" alt="image" src="https://github.com/user-attachments/assets/db47dbb1-506c-4b33-8254-f111c00120ac" />


<img width="396" height="215" alt="image" src="https://github.com/user-attachments/assets/4215b219-5bee-4e01-bef6-263d76700f6e" />


<img width="530" height="569" alt="image" src="https://github.com/user-attachments/assets/b17e2776-dd3d-49dd-86f4-6d5753327d49" />


---

## 🧩 Модули

### 1. `core` — ядро логики
- Содержит **чистую модель** `OfficeBuilding`:
  ```kotlin
  data class OfficeBuilding(
      val id: Long,
      val address: String,
      val parkingSpaces: Int? = null,
      val monthlyRent: Double? = null
  )
  ```
- Определяет **интерфейсы репозиториев** и **UseCases**.
- **Не зависит** от Android, консоли или фреймворков.

### 2. `console` — консольное приложение
- Использует `implementation(project(":core"))`.
- Реализует простой вывод зданий с фильтрацией по слову **«Тверская»**.
- Может использовать **Mock-репозиторий** или жёстко заданные данные.

### 3. `app` — Android-приложение
- Полностью следует **Clean Architecture**:
  - **Presentation**: View + ViewModel
  - **Domain**: UseCases + модели из `core`
  - **Data**: ROOM, репозитории, источники данных
- Использует **Hilt** для DI:
  - Внедряет `OfficeBuildingRepository`
  - Поддерживает несколько реализаций (ROOM, Mock)
- Реализует **полный CRUD** через экраны:
  - Список зданий
  - Добавление нового здания
  - Редактирование
  - Удаление
- Фильтрация по «Тверская» — одна из функций списка.
- Навигация через **Jetpack Navigation**.

---

## 🛠️ Технологии

| Компонент             | Технология                          |
|----------------------|-------------------------------------|
| Архитектура          | Clean Architecture + MVVM           |
| DI                   | Hilt                                |
| База данных          | ROOM                                |
| Навигация            | Jetpack Navigation                  |
| Тестирование         | JUnit, Mockito, Espresso, Truth     |
| Сборка               | Gradle (Kotlin DSL)                 |

---

## 🧪 Тестирование

- **Unit-тесты**:
  - UseCases (`core` и `domain`)
  - ViewModel
  - ROOM DAO (с `Room.inMemoryDatabaseBuilder`)
- **UI-тесты**:
  - Проверка отображения списка
  - Создание/редактирование/удаление через Espresso

---

## ▶️ Запуск

1. **Консольное приложение**:
   ```bash
   ./gradlew :console:run
   ```

2. **Android-приложение**:
   - Запустите через Android Studio или:
     ```bash
     ./gradlew :app:installDebug
     ```

---

---

> ✅ Проект демонстрирует **разделение ответственности**, **тестируемость** и **масштабируемость** — ключевые принципы современной разработки под Android.
